# ColdCache — Cold Case Connector (Python 3.10+ required by Cognee)
cognee==1.2.2
lancedb==0.26.0            # pin: 0.33 has a macOS ARM lance spill bug
# anthropic                # optional: only needed if using Claude as LLM_PROVIDER
                           # (not required for the default Ollama pipeline)
groq                       # Groq vision + Whisper client used by the hosted configuration
fastembed                  # local embeddings — no OpenAI key required
sentence-transformers      # naive vector baseline for the benchmark
scikit-learn               # cosine similarity / metrics helpers
fastapi
uvicorn[standard]
python-dotenv
python-multipart
matplotlib                 # benchmark chart
openai-whisper             # local audio transcription (tiny model, ~75MB)
opencv-python-headless     # video frame extraction
pymupdf                    # PDF text extraction + scanned page rendering
pandas                     # spreadsheet parsing
openpyxl                   # Excel support for pandas

# Optional: pgvector-based vector store (avoids embedded-LanceDB concurrency bug)
asyncpg
pgvector
